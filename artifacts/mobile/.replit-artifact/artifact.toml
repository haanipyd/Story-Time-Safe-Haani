kind = "mobile"
previewPath = "/"
title = "Storytime - Kids Audio App"
version = "1.0.0"
id = "artifacts/mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 18115

[services.development]
run = "pnpm --filter @workspace/mobile run dev"

[services.production]
build = [ "sh", "-c", "pnpm install --frozen-lockfile && pnpm --filter @workspace/mobile run build" ]
run = [ "sh", "-c", "pnpm --filter @workspace/mobile run serve" ]

[services.env]
PORT = "18115"
BASE_PATH = "/"
